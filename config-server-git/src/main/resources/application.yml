spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/wjw0215/cloud-config-demo/
          search-paths: repo
#          username:
#          password:
server:
  port: 1201